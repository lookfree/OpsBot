# KingBase 数据库驱动实现规格

> 版本：v1.0
> 创建日期：2025-12-27
> 参考实现：MySQL/PostgreSQL 驱动
> 模块目标：完整的 KingBase（人大金仓）数据库连接、查询和元数据管理

---

## 1. 概述

### 1.1 KingBase 简介

KingBase（人大金仓数据库）是国产自主可控的企业级关系型数据库管理系统，广泛应用于政府、金融、电信等关键行业。其核心特点：

- **PostgreSQL 兼容**：基于 PostgreSQL 内核开发，兼容 PostgreSQL 协议和 SQL 语法
- **国产化信创**：满足信息技术应用创新（信创）需求，支持国产 CPU 和操作系统
- **企业级特性**：支持高可用、读写分离、分布式部署
- **安全合规**：通过国家安全认证，支持三权分立、审计追踪

### 1.2 核心功能范围

| 功能模块 | 描述 |
|---------|------|
| 连接管理 | 创建连接池、测试连接、SSL 支持 |
| SQL 执行 | SELECT/INSERT/UPDATE/DELETE 语句执行 |
| 元数据获取 | 数据库、Schema、表、视图、存储过程、触发器、约束 |
| DDL 生成 | 生成 CREATE TABLE 等 DDL 语句 |
| 表操作 | 重命名表、删除表 |

### 1.3 支持版本

- KingBase V8R3 及以上
- KingBase V8R6（推荐）
- 兼容 PostgreSQL 9.x/10.x/11.x/12.x 协议

---

## 2. 技术架构

### 2.1 后端架构 (Rust/Tauri)

```
┌─────────────────────────────────────────────────────────┐
│                    Tauri Commands                        │
│  (db_connect, db_test_connection, db_execute_query...)  │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  DatabaseManager                         │
│            (services/database/mod.rs)                   │
│  ┌─────────┬─────────┬──────────┬─────────┬──────────┐ │
│  │  MySQL  │ MariaDB │PostgreSQL│ KingBase│  Oracle  │ │
│  └────┬────┴────┬────┴────┬─────┴────┬────┴────┬─────┘ │
└───────┼─────────┼─────────┼──────────┼─────────┼───────┘
        │         │         │          │         │
        ▼         ▼         ▼          ▼         ▼
┌─────────────────────────────────────────────────────────┐
│                   DatabaseDriver Trait                   │
│  execute_query, execute_update, get_tables,             │
│  get_table_structure, get_views, get_routines...        │
└─────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────┐
│              sqlx::PgPool (PostgreSQL 协议)              │
│                   复用 PostgreSQL 连接                   │
└─────────────────────────────────────────────────────────┘
```

### 2.2 前端架构 (React/TypeScript)

```
┌─────────────────────────────────────────────────────────┐
│                    UI Components                         │
│    (ConnectionTree, TableStructure, SQLEditor...)       │
└─────────────────────────┬───────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
┌─────────────────┐ ┌───────────────┐ ┌───────────────────┐
│   DataTypes     │ │  DbDialects   │ │   SQL Generator   │
│ (datatypes/)    │ │ (dbDialects/) │ │ (sql-generator/)  │
├─────────────────┤ ├───────────────┤ ├───────────────────┤
│ kingbase.ts     │ │ kingbase.ts   │ │ kingbase.ts       │
│ - 数据类型定义   │ │ - 方言配置    │ │ - DDL 生成        │
│ - 类型分类      │ │ - 语法特性    │ │ - 语法转换        │
└─────────────────┘ └───────────────┘ └───────────────────┘
```

### 2.3 目录结构

```
ZWD-OpsBot/
├── backend/src/services/database/
│   ├── mod.rs                 # 修改：添加 KingBase 支持
│   ├── traits.rs              # DatabaseDriver trait 定义
│   ├── kingbase.rs            # 新建：KingBase 驱动实现
│   └── ...
│
├── front/
│   ├── config/
│   │   ├── datatypes/
│   │   │   ├── index.ts       # 修改：导出 KingBase 类型
│   │   │   └── kingbase.ts    # 新建：数据类型定义
│   │   ├── dbDialects/
│   │   │   ├── index.ts       # 修改：导出 KingBase 方言
│   │   │   └── kingbase.ts    # 新建：方言配置
│   │   └── databaseTypes.ts   # 修改：添加 KingBase 类型
│   │
│   ├── components/icons/
│   │   └── DatabaseIcons.tsx  # 修改：添加 KingBase 图标
│   │
│   └── utils/sql-generator/
│       ├── index.ts           # 修改：导出 KingBase 生成器
│       └── kingbase.ts        # 新建：SQL 生成器
│
└── public/assets/icons/
    └── kingbase-icon.svg      # 新建：KingBase 图标
```

---

## 3. 后端实现 (Rust)

### 3.1 KingBase 驱动结构

**文件**: `backend/src/services/database/kingbase.rs`

```rust
//! KingBase database driver implementation
//!
//! KingBase is compatible with PostgreSQL protocol,
//! so we reuse sqlx::PgPool for connections.

use std::collections::HashMap;
use std::time::Instant;

use async_trait::async_trait;
use sqlx::postgres::{PgPool, PgPoolOptions, PgRow};
use sqlx::{Column, Row, TypeInfo};
use urlencoding::encode;

use crate::models::{
    CheckConstraintInfo, DatabaseObjectsCount, ForeignKeyInfo, QueryColumn, QueryResult,
    RoutineInfo, TableInfo, TableOptions, TableStructure, TriggerInfo, ViewInfo,
};

use super::traits::{build_column_detail, build_index_map, DatabaseDriver};

/// KingBase database driver
pub struct KingBaseDriver {
    pool: PgPool,
}
```

### 3.2 关键方法实现

#### 3.2.1 连接管理

```rust
impl KingBaseDriver {
    /// Create a new KingBase connection
    pub async fn connect(
        host: &str,
        port: u16,
        username: &str,
        password: &str,
        database: &str,
    ) -> Result<Self, String> {
        // KingBase 使用 PostgreSQL 协议，默认端口 54321
        let url = format!(
            "postgres://{}:{}@{}:{}/{}",
            encode(username), encode(password), host, port, database
        );

        log::info!("Connecting to KingBase: {}:{}/{}", host, port, database);

        let pool = PgPoolOptions::new()
            .max_connections(10)
            .min_connections(2)
            .acquire_timeout(std::time::Duration::from_secs(10))
            .connect(&url)
            .await
            .map_err(|e| format!("Failed to connect to KingBase: {}", e))?;

        log::info!("KingBase connection established successfully");
        Ok(Self { pool })
    }

    /// Test connection without keeping it open
    pub async fn test_connection(
        host: &str,
        port: u16,
        username: &str,
        password: &str,
        database: &str,
    ) -> Result<(), String> {
        let url = format!(
            "postgres://{}:{}@{}:{}/{}",
            encode(username), encode(password), host, port, database
        );

        let pool = PgPoolOptions::new()
            .max_connections(1)
            .acquire_timeout(std::time::Duration::from_secs(10))
            .connect(&url)
            .await
            .map_err(|e| format!("Connection test failed: {}", e))?;

        // 测试查询
        sqlx::query("SELECT 1")
            .execute(&pool)
            .await
            .map_err(|e| format!("Query test failed: {}", e))?;

        pool.close().await;
        Ok(())
    }
}
```

#### 3.2.2 类型转换

```rust
impl KingBaseDriver {
    fn get_column_value(&self, row: &PgRow, index: usize, type_name: &str) -> serde_json::Value {
        match type_name {
            // 整数类型
            "INT8" | "INT4" | "INT2" | "SERIAL" | "BIGSERIAL" => row
                .try_get::<i64, _>(index)
                .map(serde_json::Value::from)
                .unwrap_or(serde_json::Value::Null),
            // 浮点类型
            "FLOAT8" | "FLOAT4" | "NUMERIC" => row
                .try_get::<f64, _>(index)
                .map(serde_json::Value::from)
                .unwrap_or(serde_json::Value::Null),
            // 布尔类型
            "BOOL" => row
                .try_get::<bool, _>(index)
                .map(serde_json::Value::from)
                .unwrap_or(serde_json::Value::Null),
            // JSON 类型
            "JSON" | "JSONB" => row
                .try_get::<serde_json::Value, _>(index)
                .unwrap_or(serde_json::Value::Null),
            // 其他类型转为字符串
            _ => row
                .try_get::<String, _>(index)
                .map(serde_json::Value::from)
                .unwrap_or(serde_json::Value::Null),
        }
    }
}
```

### 3.3 DatabaseDriver Trait 实现

```rust
#[async_trait]
impl DatabaseDriver for KingBaseDriver {
    async fn execute_query(&self, sql: &str) -> Result<QueryResult, String> {
        let start = Instant::now();

        let rows: Vec<PgRow> = sqlx::query(sql)
            .fetch_all(&self.pool)
            .await
            .map_err(|e| format!("Query failed: {}", e))?;

        let execution_time_ms = start.elapsed().as_millis() as u64;

        let columns: Vec<QueryColumn> = if let Some(first_row) = rows.first() {
            first_row
                .columns()
                .iter()
                .map(|col| QueryColumn {
                    name: col.name().to_string(),
                    column_type: col.type_info().name().to_string(),
                    nullable: true,
                })
                .collect()
        } else {
            vec![]
        };

        let data: Vec<Vec<serde_json::Value>> = rows
            .iter()
            .map(|row| {
                row.columns()
                    .iter()
                    .enumerate()
                    .map(|(i, col)| self.get_column_value(row, i, col.type_info().name()))
                    .collect()
            })
            .collect();

        Ok(QueryResult {
            columns,
            rows: data,
            affected_rows: rows.len() as u64,
            execution_time_ms,
        })
    }

    async fn execute_update(&self, sql: &str) -> Result<QueryResult, String> {
        let start = Instant::now();

        let result = sqlx::query(sql)
            .execute(&self.pool)
            .await
            .map_err(|e| format!("Execute failed: {}", e))?;

        Ok(QueryResult {
            columns: vec![],
            rows: vec![],
            affected_rows: result.rows_affected(),
            execution_time_ms: start.elapsed().as_millis() as u64,
        })
    }

    async fn get_databases(&self) -> Result<Vec<String>, String> {
        let sql = "SELECT datname FROM pg_database WHERE datistemplate = false ORDER BY datname";

        let rows: Vec<PgRow> = sqlx::query(sql)
            .fetch_all(&self.pool)
            .await
            .map_err(|e| format!("Failed to get databases: {}", e))?;

        Ok(rows
            .iter()
            .filter_map(|row| row.try_get::<String, _>(0).ok())
            .collect())
    }

    async fn get_schemas(&self, _database: Option<&str>) -> Result<Vec<String>, String> {
        let sql = "SELECT schema_name FROM information_schema.schemata \
                   WHERE schema_name NOT IN ('pg_catalog', 'information_schema', 'pg_toast', 'sys_catalog') \
                   ORDER BY schema_name";

        let rows: Vec<PgRow> = sqlx::query(sql)
            .fetch_all(&self.pool)
            .await
            .map_err(|e| format!("Failed to get schemas: {}", e))?;

        Ok(rows
            .iter()
            .filter_map(|row| row.try_get::<String, _>(0).ok())
            .collect())
    }

    async fn get_tables(&self, _database: &str, schema: Option<&str>) -> Result<Vec<TableInfo>, String> {
        let schema_name = schema.unwrap_or("public");
        let sql = "SELECT table_name FROM information_schema.tables \
                   WHERE table_schema = $1 AND table_type = 'BASE TABLE' ORDER BY table_name";

        let rows: Vec<PgRow> = sqlx::query(sql)
            .bind(schema_name)
            .fetch_all(&self.pool)
            .await
            .map_err(|e| format!("Failed to get tables: {}", e))?;

        Ok(rows
            .iter()
            .filter_map(|row| {
                Some(TableInfo {
                    name: row.try_get("table_name").ok()?,
                    table_type: "BASE TABLE".to_string(),
                    row_count: None,
                })
            })
            .collect())
    }

    // ... 其他方法参考 PostgreSQL 驱动实现
}
```

### 3.4 元数据查询 SQL

#### 获取表结构

```sql
SELECT c.column_name, c.data_type, c.is_nullable, c.column_default,
       c.character_maximum_length, c.numeric_precision, c.numeric_scale,
       CASE WHEN pk.column_name IS NOT NULL THEN 'PRI' ELSE '' END as key,
       COALESCE(pgd.description, '') as comment
FROM information_schema.columns c
LEFT JOIN (
    SELECT ku.column_name FROM information_schema.table_constraints tc
    JOIN information_schema.key_column_usage ku
        ON tc.constraint_name = ku.constraint_name
    WHERE tc.table_schema = $1 AND tc.table_name = $2
        AND tc.constraint_type = 'PRIMARY KEY'
) pk ON c.column_name = pk.column_name
LEFT JOIN pg_catalog.pg_statio_all_tables st
    ON st.schemaname = c.table_schema AND st.relname = c.table_name
LEFT JOIN pg_catalog.pg_description pgd
    ON pgd.objoid = st.relid AND pgd.objsubid = c.ordinal_position
WHERE c.table_schema = $1 AND c.table_name = $2
ORDER BY c.ordinal_position
```

#### 获取索引

```sql
SELECT i.relname as index_name, a.attname as column_name,
       ix.indisunique as is_unique, am.amname as index_type
FROM pg_class t
JOIN pg_index ix ON t.oid = ix.indrelid
JOIN pg_class i ON i.oid = ix.indexrelid
JOIN pg_am am ON i.relam = am.oid
JOIN pg_attribute a ON a.attrelid = t.oid AND a.attnum = ANY(ix.indkey)
JOIN pg_namespace n ON n.oid = t.relnamespace
WHERE n.nspname = $1 AND t.relname = $2
ORDER BY i.relname, a.attnum
```

#### 获取外键

```sql
SELECT tc.constraint_name as name, kcu.column_name as col,
       ccu.table_name as ref_table, ccu.column_name as ref_col,
       rc.delete_rule as on_delete, rc.update_rule as on_update
FROM information_schema.table_constraints tc
JOIN information_schema.key_column_usage kcu
    ON tc.constraint_name = kcu.constraint_name
    AND tc.table_schema = kcu.table_schema
JOIN information_schema.constraint_column_usage ccu
    ON ccu.constraint_name = tc.constraint_name
    AND ccu.table_schema = tc.table_schema
JOIN information_schema.referential_constraints rc
    ON rc.constraint_name = tc.constraint_name
    AND rc.constraint_schema = tc.table_schema
WHERE tc.constraint_type = 'FOREIGN KEY'
    AND tc.table_schema = $1 AND tc.table_name = $2
ORDER BY tc.constraint_name
```

#### 获取触发器

```sql
SELECT t.tgname as trigger_name,
       CASE WHEN t.tgtype & 2 = 2 THEN 'BEFORE' ELSE 'AFTER' END as timing,
       CONCAT_WS(' OR ',
           CASE WHEN t.tgtype & 4 = 4 THEN 'INSERT' END,
           CASE WHEN t.tgtype & 8 = 8 THEN 'DELETE' END,
           CASE WHEN t.tgtype & 16 = 16 THEN 'UPDATE' END,
           CASE WHEN t.tgtype & 32 = 32 THEN 'TRUNCATE' END
       ) as event,
       pg_get_triggerdef(t.oid) as statement
FROM pg_trigger t
JOIN pg_class c ON c.oid = t.tgrelid
JOIN pg_namespace n ON n.oid = c.relnamespace
WHERE NOT t.tgisinternal AND n.nspname = $1 AND c.relname = $2
```

---

## 4. 前端配置

### 4.1 数据类型定义

**文件**: `front/config/datatypes/kingbase.ts`

```typescript
/**
 * KingBase 数据类型定义
 *
 * KingBase 基于 PostgreSQL，兼容 PostgreSQL 数据类型，
 * 同时支持一些 KingBase 特有的类型和扩展。
 */

import type { DataTypeDefinition } from './types'
import { baseTypes, TYPE_COLORS, validators } from './base'

/**
 * KingBase 特有的数据类型
 */
const kingbaseSpecificTypes: Record<string, DataTypeDefinition> = {
  // 序列类型（自增）
  SERIAL: {
    type: 'SERIAL',
    color: TYPE_COLORS.integer,
    checkDefault: validators.isInteger,
    hasCheck: true,
    isSized: false,
    hasPrecision: false,
    canIncrement: true,
    category: 'integer',
  },
  SMALLSERIAL: {
    type: 'SMALLSERIAL',
    color: TYPE_COLORS.integer,
    checkDefault: validators.isInteger,
    hasCheck: true,
    isSized: false,
    hasPrecision: false,
    canIncrement: true,
    category: 'integer',
  },
  BIGSERIAL: {
    type: 'BIGSERIAL',
    color: TYPE_COLORS.integer,
    checkDefault: validators.isInteger,
    hasCheck: true,
    isSized: false,
    hasPrecision: false,
    canIncrement: true,
    category: 'integer',
  },

  // 整数类型
  INTEGER: {
    type: 'INTEGER',
    color: TYPE_COLORS.integer,
    checkDefault: validators.isInteger,
    hasCheck: true,
    isSized: false,
    hasPrecision: false,
    canIncrement: true,
    category: 'integer',
  },

  // 浮点类型
  DOUBLE_PRECISION: {
    type: 'DOUBLE PRECISION',
    color: TYPE_COLORS.decimal,
    checkDefault: validators.isDecimal,
    hasCheck: true,
    isSized: false,
    hasPrecision: false,
    category: 'decimal',
  },
  MONEY: {
    type: 'MONEY',
    color: TYPE_COLORS.decimal,
    checkDefault: validators.isDecimal,
    hasCheck: true,
    isSized: false,
    hasPrecision: false,
    category: 'decimal',
  },

  // 字符串类型
  CHARACTER: {
    type: 'CHARACTER',
    color: TYPE_COLORS.string,
    checkDefault: validators.isValidString,
    hasCheck: true,
    isSized: true,
    hasPrecision: false,
    defaultSize: 1,
    hasQuotes: true,
    category: 'string',
  },
  CHARACTER_VARYING: {
    type: 'CHARACTER VARYING',
    color: TYPE_COLORS.string,
    checkDefault: validators.isValidString,
    hasCheck: true,
    isSized: true,
    hasPrecision: false,
    defaultSize: 255,
    hasQuotes: true,
    category: 'string',
  },

  // UUID 类型
  UUID: {
    type: 'UUID',
    color: TYPE_COLORS.string,
    checkDefault: validators.isUUID,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    hasQuotes: true,
    category: 'string',
  },

  // JSON 类型
  JSONB: {
    type: 'JSONB',
    color: TYPE_COLORS.json,
    checkDefault: validators.isJSON,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'json',
  },

  // 二进制类型
  BYTEA: {
    type: 'BYTEA',
    color: TYPE_COLORS.binary,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'binary',
  },

  // 日期时间类型
  TIMESTAMPTZ: {
    type: 'TIMESTAMPTZ',
    color: TYPE_COLORS.datetime,
    checkDefault: validators.isDateTime,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    hasQuotes: true,
    category: 'datetime',
  },
  TIMESTAMP_WITH_TIME_ZONE: {
    type: 'TIMESTAMP WITH TIME ZONE',
    color: TYPE_COLORS.datetime,
    checkDefault: validators.isDateTime,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    hasQuotes: true,
    category: 'datetime',
  },
  INTERVAL: {
    type: 'INTERVAL',
    color: TYPE_COLORS.datetime,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    hasQuotes: true,
    category: 'datetime',
  },

  // 网络类型
  CIDR: {
    type: 'CIDR',
    color: TYPE_COLORS.network,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    hasQuotes: true,
    category: 'network',
  },
  INET: {
    type: 'INET',
    color: TYPE_COLORS.network,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    hasQuotes: true,
    category: 'network',
  },
  MACADDR: {
    type: 'MACADDR',
    color: TYPE_COLORS.network,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    hasQuotes: true,
    category: 'network',
  },

  // 几何类型
  POINT: {
    type: 'POINT',
    color: TYPE_COLORS.geometric,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'geometric',
  },
  LINE: {
    type: 'LINE',
    color: TYPE_COLORS.geometric,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'geometric',
  },
  POLYGON: {
    type: 'POLYGON',
    color: TYPE_COLORS.geometric,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'geometric',
  },
  CIRCLE: {
    type: 'CIRCLE',
    color: TYPE_COLORS.geometric,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'geometric',
  },

  // 全文搜索类型
  TSVECTOR: {
    type: 'TSVECTOR',
    color: TYPE_COLORS.other,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'other',
  },
  TSQUERY: {
    type: 'TSQUERY',
    color: TYPE_COLORS.other,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'other',
  },

  // 数组类型
  ARRAY: {
    type: 'ARRAY',
    color: TYPE_COLORS.other,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'other',
  },

  // XML 类型
  XML: {
    type: 'XML',
    color: TYPE_COLORS.other,
    checkDefault: validators.any,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'other',
  },

  // 布尔类型
  BOOL: {
    type: 'BOOL',
    color: TYPE_COLORS.boolean,
    checkDefault: validators.isBoolean,
    hasCheck: false,
    isSized: false,
    hasPrecision: false,
    category: 'boolean',
  },
}

/**
 * KingBase 完整数据类型映射表
 */
export const kingbaseTypes: Record<string, DataTypeDefinition> = {
  ...baseTypes,
  ...kingbaseSpecificTypes,
}

/**
 * KingBase 数据类型列表（按分类）
 */
export const kingbaseTypeCategories = {
  整数: ['SMALLINT', 'INTEGER', 'INT', 'BIGINT', 'SMALLSERIAL', 'SERIAL', 'BIGSERIAL'],
  小数: ['DECIMAL', 'NUMERIC', 'REAL', 'DOUBLE_PRECISION', 'MONEY'],
  字符串: ['CHAR', 'CHARACTER', 'VARCHAR', 'CHARACTER_VARYING', 'TEXT', 'UUID'],
  日期时间: ['DATE', 'TIME', 'TIMESTAMP', 'TIMESTAMPTZ', 'TIMESTAMP_WITH_TIME_ZONE', 'INTERVAL'],
  布尔: ['BOOLEAN', 'BOOL'],
  二进制: ['BYTEA'],
  JSON: ['JSON', 'JSONB'],
  几何: ['POINT', 'LINE', 'POLYGON', 'CIRCLE'],
  网络: ['CIDR', 'INET', 'MACADDR'],
  全文搜索: ['TSVECTOR', 'TSQUERY'],
  其他: ['ARRAY', 'XML'],
}
```

### 4.2 方言配置

**文件**: `front/config/dbDialects/kingbase.ts`

```typescript
/**
 * KingBase 方言配置
 *
 * KingBase 基于 PostgreSQL，语法高度兼容 PostgreSQL，
 * 但在某些细节上有自己的特性。
 */

import type { DatabaseDialectConfig } from './types'

export const kingbaseDialect: DatabaseDialectConfig = {
  id: 'kingbase',
  name: 'KingBase',

  // 标识符和字符串引用
  quoteIdentifier: (name: string) => `"${name.replace(/"/g, '""')}"`,
  quoteString: (value: string) => `'${value.replace(/'/g, "''")}'`,

  // 表选项
  tableOptions: {
    engines: [], // KingBase 不使用存储引擎概念
    charsets: ['UTF8', 'GBK', 'GB18030', 'EUC_CN'],
    collations: [
      'zh_CN.UTF-8',
      'C',
      'POSIX',
      'default',
    ],
    supportsSchema: true,
    supportsTablespace: true,
    supportsTableComment: true,
    supportsColumnComment: true,
  },

  // 索引选项
  indexOptions: {
    types: ['BTREE', 'HASH', 'GIN', 'GIST', 'SPGIST', 'BRIN'],
    supportsFulltext: true,
    supportsSpatial: true,
    supportsHash: true,
    supportsUnique: true,
    supportsFunctional: true,
    supportsPartial: true,
    supportsConcurrent: true,
  },

  // 约束选项
  constraintOptions: {
    foreignKeyActions: ['NO ACTION', 'RESTRICT', 'CASCADE', 'SET NULL', 'SET DEFAULT'],
    supportsCheck: true,
    supportsExclusion: true,
    supportsNamedConstraints: true,
    supportsDeferrable: true,
  },

  // 自增配置
  autoIncrement: {
    keyword: 'SERIAL',
    alternateKeyword: 'IDENTITY',
    supportsStartWith: true,
    supportsIncrementBy: true,
    usesSequence: true,
  },

  // 注释语法
  comment: {
    inline: false,
    separate: true,
    syntax: "COMMENT ON {object_type} {object_name} IS '{comment}'",
  },

  // SQL 语法
  syntax: {
    batchSeparator: ';',
    supportsIfNotExists: true,
    supportsCascade: true,
    supportsLimitOffset: true,
    paginationType: 'limit-offset',
    supportsReturning: true,
    supportsUpsert: true,
    upsertSyntax: 'ON CONFLICT',
    supportsWindow: true,
    supportsCTE: true,
    supportsLateral: true,
  },

  // 默认值
  defaults: {
    schema: 'public',
    charset: 'UTF8',
    collation: 'default',
  },
}
```

### 4.3 数据库类型配置

**文件**: `front/config/databaseTypes.ts`（修改）

```typescript
// 添加 KingBase 配置
{
  id: 'kingbase',
  name: 'KingBase',
  icon: '/assets/icons/kingbase-icon.svg',
  defaultPort: 54321,
  supportsSchema: true,
  description: '人大金仓数据库',
}
```

### 4.4 图标组件

**文件**: `front/components/icons/DatabaseIcons.tsx`（修改）

```typescript
// KingBase Icon
export function KingBaseIcon({ className }: IconProps) {
  return (
    <img
      src="/assets/icons/kingbase-icon.svg"
      alt="KingBase"
      className={cn('w-4 h-4', className)}
    />
  )
}

// 在 getDatabaseIcon 函数中添加
case 'kingbase':
  return KingBaseIcon
```

---

## 5. 实现步骤

### Phase 1: 后端基础实现

| 步骤 | 任务 | 文件 |
|-----|------|------|
| 1.1 | 创建 KingBase 驱动结构体 | `backend/src/services/database/kingbase.rs` |
| 1.2 | 实现连接管理方法 | `kingbase.rs` |
| 1.3 | 实现 DatabaseDriver trait | `kingbase.rs` |
| 1.4 | 在 mod.rs 中注册驱动 | `backend/src/services/database/mod.rs` |
| 1.5 | 添加 KingBase 到 DatabaseType 枚举 | `backend/src/models/mod.rs` |

### Phase 2: 前端配置实现

| 步骤 | 任务 | 文件 |
|-----|------|------|
| 2.1 | 创建数据类型定义 | `front/config/datatypes/kingbase.ts` |
| 2.2 | 在 index.ts 中导出类型 | `front/config/datatypes/index.ts` |
| 2.3 | 创建方言配置 | `front/config/dbDialects/kingbase.ts` |
| 2.4 | 在 index.ts 中导出方言 | `front/config/dbDialects/index.ts` |
| 2.5 | 添加数据库类型配置 | `front/config/databaseTypes.ts` |
| 2.6 | 添加图标组件 | `front/components/icons/DatabaseIcons.tsx` |
| 2.7 | 添加图标文件 | `public/assets/icons/kingbase-icon.svg` |

### Phase 3: SQL 生成器

| 步骤 | 任务 | 文件 |
|-----|------|------|
| 3.1 | 创建 KingBase SQL 生成器 | `front/utils/sql-generator/kingbase.ts` |
| 3.2 | 在 index.ts 中导出生成器 | `front/utils/sql-generator/index.ts` |

### Phase 4: 集成测试

| 步骤 | 任务 |
|-----|------|
| 4.1 | 连接测试 |
| 4.2 | 元数据获取测试 |
| 4.3 | SQL 执行测试 |
| 4.4 | DDL 生成测试 |

---

## 6. 文件清单

### 新建文件

| 文件路径 | 说明 |
|---------|------|
| `backend/src/services/database/kingbase.rs` | KingBase 驱动实现 |
| `front/config/datatypes/kingbase.ts` | 数据类型定义 |
| `front/config/dbDialects/kingbase.ts` | 方言配置 |
| `front/utils/sql-generator/kingbase.ts` | SQL 生成器 |
| `public/assets/icons/kingbase-icon.svg` | 图标文件 |

### 修改文件

| 文件路径 | 修改内容 |
|---------|----------|
| `backend/src/services/database/mod.rs` | 添加 KingBase 驱动注册 |
| `backend/src/models/mod.rs` | 添加 KingBase 到 DatabaseType |
| `front/config/datatypes/index.ts` | 导出 kingbaseTypes |
| `front/config/dbDialects/index.ts` | 导出 kingbaseDialect |
| `front/config/databaseTypes.ts` | 添加 KingBase 配置 |
| `front/components/icons/DatabaseIcons.tsx` | 添加 KingBaseIcon |
| `front/types/connection.ts` | 添加 'kingbase' 到 DatabaseType |
| `front/services/database.ts` | 添加 'kingbase' 到 DatabaseType |

---

## 7. 验收标准

### 功能验收

- [ ] 能成功连接 KingBase 数据库（包括 SSL 连接）
- [ ] 能获取数据库列表
- [ ] 能获取 Schema 列表
- [ ] 能获取表列表
- [ ] 能获取表结构（列、索引、约束）
- [ ] 能获取视图列表
- [ ] 能获取存储过程/函数列表
- [ ] 能获取触发器列表
- [ ] 能执行 SELECT 查询并返回结果
- [ ] 能执行 INSERT/UPDATE/DELETE 并返回影响行数
- [ ] 能生成正确的 CREATE TABLE DDL
- [ ] 能重命名表
- [ ] 能删除表
- [ ] 前端能正确显示 KingBase 图标
- [ ] 前端能正确显示 KingBase 数据类型
- [ ] ER 图设计器支持 KingBase 语法

### 兼容性验收

- [ ] 与 KingBase V8R3 兼容
- [ ] 与 KingBase V8R6 兼容
- [ ] 支持中文表名/列名
- [ ] 支持中文注释

### 性能验收

- [ ] 连接建立时间 < 3 秒
- [ ] 简单查询响应时间 < 100ms
- [ ] 元数据获取时间 < 500ms

---

## 8. 附录

### 8.1 KingBase 与 PostgreSQL 差异

| 特性 | PostgreSQL | KingBase |
|-----|-----------|----------|
| 默认端口 | 5432 | 54321 |
| 系统 Schema | pg_catalog | sys_catalog |
| 字符集 | UTF8 | UTF8/GBK/GB18030 |
| 安全特性 | 标准 | 三权分立、审计 |

### 8.2 参考资料

- [KingBase 官方文档](https://www.kingbase.com.cn/)
- [PostgreSQL 文档](https://www.postgresql.org/docs/)
- [sqlx-postgres 文档](https://docs.rs/sqlx/latest/sqlx/postgres/index.html)
