# Elasticsearch 搜索引擎管理功能设计文档

## 1. 概述

### 1.1 目标
为 ZWD-OpsBot 添加 Elasticsearch 搜索引擎管理支持，作为中间件模块的搜索引擎实现。设计时需考虑：
- **复用现有架构**：使用已定义的 `MiddlewareConnection` 和 `elasticsearchConfig` 类型
- **策略模式解耦**：便于后续扩展 OpenSearch、Solr 等搜索引擎
- **遵循开发规范**：函数不超过80行，单文件不超过800行
- **版本兼容**：支持 ES 7.x/8.x 主流版本

### 1.2 核心功能

| 功能模块 | 说明 |
|---------|------|
| 连接管理 | 支持单节点/集群、多种认证方式、SSL/TLS |
| 集群监控 | 健康状态、节点信息、分片分配、集群设置 |
| 索引管理 | 列表、创建、删除、打开/关闭、映射、设置 |
| 文档操作 | 获取、创建、更新、删除、批量操作 |
| 查询分析 | DSL 查询、SQL 查询（ES 6.3+） |

### 1.3 与现有架构的集成点

| 集成点 | 现有实现 | Elasticsearch 集成方式 |
|-------|---------|----------------------|
| 连接类型 | `MiddlewareConnection` | 添加 `elasticsearchConfig` 配置 |
| 连接树 | `ConnectionTree.tsx` | 在中间件节点下显示 ES 子树 |
| 状态管理 | `connectionStore.ts` | 扩展中间件相关状态 |
| 服务层 | `services/middleware/` | 添加 ES 驱动实现 |

### 1.4 类型定义扩展（front/types/connection.ts）

```typescript
export interface MiddlewareConnection extends ConnectionBase {
  moduleType: ModuleType.Middleware
  middlewareType: MiddlewareType
  // ... 已有配置 ...
  elasticsearchConfig?: {
    nodes: string[]                    // 节点列表 ["http://localhost:9200"]
    version?: '5' | '6' | '7' | '8' | '9'  // ES 版本（可选，支持自动检测）
    authType: 'none' | 'basic' | 'apiKey' | 'cloud'  // 认证方式
    username?: string                  // 基础认证用户名
    password?: string                  // 基础认证密码
    apiKey?: string                    // API Key (id:key 格式)
    cloudId?: string                   // Elastic Cloud ID
    tls?: {
      enabled: boolean                 // 是否启用 TLS
      rejectUnauthorized: boolean      // 是否验证证书
      ca?: string                      // CA 证书路径
    }
    requestTimeout?: number            // 请求超时 (毫秒)
  }
}
```

**节点输入格式说明**：
- 集群模式使用多行文本框输入，每行一个节点
- 支持两种格式：
  - `host:port` - 自动添加 http:// 前缀
  - `http(s)://host:port` - 完整 URL 格式
- 示例：
  ```
  192.168.1.100:9200
  http://192.168.1.101:9200
  https://192.168.1.102:9200
  ```

---

## 2. 架构设计

### 2.1 后端架构（策略模式）

扩展现有 `services/middleware/` 目录：

```
backend/src/services/middleware/
├── mod.rs                    # 模块入口 + MiddlewareService
├── traits.rs                 # SearchEngineDriver trait 定义
├── kafka/                    # 已有 Kafka 实现
├── elasticsearch/
│   ├── mod.rs               # ES 模块入口
│   ├── driver.rs            # ElasticsearchDriver 实现 (< 400 行)
│   ├── cluster.rs           # 集群操作 (< 300 行)
│   ├── index.rs             # 索引操作 (< 300 行)
│   ├── document.rs          # 文档操作 (< 300 行)
│   └── query.rs             # 查询操作 (< 200 行)
└── opensearch/              # 未来扩展：OpenSearch
    └── ...
```

### 2.2 前端架构

```
front/
├── components/middleware/
│   └── elasticsearch/
│       ├── ElasticsearchOverview.tsx      # 集群概览
│       ├── ElasticsearchIndices.tsx       # 索引管理
│       ├── ElasticsearchDocuments.tsx     # 文档管理
│       ├── ElasticsearchQuery.tsx         # 查询分析
│       └── ElasticsearchConnectionDialog.tsx  # 连接配置
├── services/
│   └── middleware.ts                      # 扩展 ES API 封装
└── types/
    └── middleware.ts                      # 扩展 ES 类型
```

### 2.3 模块依赖关系

```
┌────────────────────────────────────────────────────────────────┐
│                        前端 (React)                             │
├────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐   ┌─────────────────┐   ┌──────────────┐ │
│  │ConnectionTree   │──▶│ middlewareStore │──▶│ middleware.ts│ │
│  │ (现有组件扩展)   │   │  (Zustand)      │   │ (API 封装)   │ │
│  └─────────────────┘   └─────────────────┘   └──────┬───────┘ │
│                                                      │         │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │         Elasticsearch UI 组件                            │  │
│  │  Overview | Indices | Documents | Query                  │  │
│  └─────────────────────────────────────────────────────────┘  │
├────────────────────────────────────────────────────────────────┤
│                        Tauri IPC                               │
├────────────────────────────────────────────────────────────────┤
│                        后端 (Rust)                              │
├────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐   ┌─────────────────────────────────────┐ │
│  │ commands/       │──▶│ services/middleware/                │ │
│  │ middleware.rs   │   │                                     │ │
│  └─────────────────┘   │  ┌─────────────────────────────┐   │ │
│                        │  │ MiddlewareService           │   │ │
│                        │  │ (连接池管理 + 路由)          │   │ │
│                        │  └──────────────┬──────────────┘   │ │
│                        │                 │                   │ │
│                        │    ┌────────────┼────────────┐     │ │
│                        │    ▼            ▼            ▼     │ │
│                        │ ┌──────┐ ┌────────────┐ ┌────────┐│ │
│                        │ │Kafka │ │Elasticsearch│ │OpenSearch││ │
│                        │ │Driver│ │Driver      │ │Driver  ││ │
│                        │ └──────┘ └────────────┘ └────────┘│ │
│                        └─────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

---

## 3. 后端实现

### 3.1 搜索引擎 Trait 定义

```rust
// backend/src/services/middleware/traits.rs

use async_trait::async_trait;
use serde::{Deserialize, Serialize};

/// 搜索引擎驱动 Trait
#[async_trait]
pub trait SearchEngineDriver: Send + Sync {
    // ============ 连接管理 ============
    async fn test_connection(&self) -> Result<EsConnectionInfo, String>;
    async fn disconnect(&self) -> Result<(), String>;

    // ============ 集群信息 ============
    async fn get_cluster_health(&self) -> Result<EsClusterHealth, String>;
    async fn get_cluster_stats(&self) -> Result<EsClusterStats, String>;
    async fn get_nodes(&self) -> Result<Vec<EsNodeInfo>, String>;
    async fn get_shards(&self) -> Result<Vec<EsShardInfo>, String>;

    // ============ 索引操作 ============
    async fn get_indices(&self, pattern: Option<&str>) -> Result<Vec<EsIndexSummary>, String>;
    async fn get_index_mapping(&self, index: &str) -> Result<serde_json::Value, String>;
    async fn get_index_settings(&self, index: &str) -> Result<serde_json::Value, String>;
    async fn get_index_stats(&self, index: &str) -> Result<EsIndexStats, String>;
    async fn create_index(&self, request: EsCreateIndexRequest) -> Result<(), String>;
    async fn delete_index(&self, index: &str) -> Result<(), String>;
    async fn open_index(&self, index: &str) -> Result<(), String>;
    async fn close_index(&self, index: &str) -> Result<(), String>;
    async fn refresh_index(&self, index: &str) -> Result<(), String>;

    // ============ 索引映射和设置修改 ============
    /// 更新索引映射（只能添加新字段，不能修改已有字段类型）
    async fn update_index_mapping(&self, index: &str, mapping: serde_json::Value) -> Result<(), String>;
    /// 更新索引设置（仅支持动态设置，如 number_of_replicas、refresh_interval 等）
    async fn update_index_settings(&self, index: &str, settings: serde_json::Value) -> Result<(), String>;

    // ============ 文档操作 ============
    async fn get_document(&self, index: &str, id: &str) -> Result<EsDocument, String>;
    async fn create_document(&self, request: EsCreateDocRequest) -> Result<EsDocResponse, String>;
    async fn update_document(&self, request: EsUpdateDocRequest) -> Result<EsDocResponse, String>;
    async fn delete_document(&self, index: &str, id: &str) -> Result<(), String>;
    async fn bulk_operation(&self, operations: Vec<EsBulkOperation>) -> Result<EsBulkResponse, String>;

    // ============ 查询操作 ============
    async fn search(&self, request: EsSearchRequest) -> Result<EsSearchResponse, String>;
    async fn sql_query(&self, query: &str) -> Result<EsSqlResponse, String>;
}

/// 集群健康状态
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EsClusterHealth {
    pub cluster_name: String,
    pub status: String,              // green | yellow | red
    pub number_of_nodes: i32,
    pub number_of_data_nodes: i32,
    pub active_primary_shards: i32,
    pub active_shards: i32,
    pub relocating_shards: i32,
    pub initializing_shards: i32,
    pub unassigned_shards: i32,
}

/// 节点信息
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EsNodeInfo {
    pub id: String,
    pub name: String,
    pub ip: String,
    pub roles: Vec<String>,          // master, data, ingest, ml
    pub version: String,
    pub heap_percent: i32,
    pub disk_percent: i32,
    pub cpu_percent: i32,
}

/// 分片信息
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EsShardInfo {
    pub index: String,
    pub shard: i32,
    pub primary: bool,
    pub state: String,               // STARTED, RELOCATING, INITIALIZING, UNASSIGNED
    pub docs: i64,
    pub size: String,
    pub node: Option<String>,
}

/// 索引概要
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EsIndexSummary {
    pub name: String,
    pub health: String,              // green | yellow | red
    pub status: String,              // open | close
    pub uuid: String,
    pub docs_count: i64,
    pub docs_deleted: i64,
    pub store_size: String,
    pub primary_shards: i32,
    pub replica_shards: i32,
}

/// 文档
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EsDocument {
    pub index: String,
    pub id: String,
    pub version: i64,
    pub source: serde_json::Value,
}

/// 搜索响应
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EsSearchResponse {
    pub took: i64,
    pub timed_out: bool,
    pub total: i64,
    pub max_score: Option<f64>,
    pub hits: Vec<EsSearchHit>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EsSearchHit {
    pub index: String,
    pub id: String,
    pub score: Option<f64>,
    pub source: serde_json::Value,
}
```

### 3.2 Elasticsearch Driver 实现

```rust
// backend/src/services/middleware/elasticsearch/driver.rs

use elasticsearch::{
    Elasticsearch,
    http::transport::{Transport, SingleNodeConnectionPool, TransportBuilder},
    auth::Credentials,
    cert::CertificateValidation,
};
use url::Url;
use std::sync::Arc;
use crate::services::middleware::traits::*;

pub struct ElasticsearchDriver {
    client: Elasticsearch,
    config: EsConfig,
}

impl ElasticsearchDriver {
    pub async fn connect(config: EsConfig) -> Result<Self, String> {
        let transport = Self::build_transport(&config)?;
        let client = Elasticsearch::new(transport);

        // 验证连接
        let driver = Self { client, config };
        driver.test_connection().await?;

        Ok(driver)
    }

    fn build_transport(config: &EsConfig) -> Result<Transport, String> {
        let url = Url::parse(&config.nodes[0])
            .map_err(|e| format!("Invalid URL: {}", e))?;

        let conn_pool = SingleNodeConnectionPool::new(url);
        let mut builder = TransportBuilder::new(conn_pool);

        // 配置认证
        match &config.auth_type {
            AuthType::Basic { username, password } => {
                builder = builder.auth(Credentials::Basic(
                    username.clone(),
                    password.clone(),
                ));
            }
            AuthType::ApiKey { api_key } => {
                builder = builder.auth(Credentials::ApiKey(
                    api_key.clone(),
                ));
            }
            AuthType::None => {}
        }

        // 配置 TLS
        if let Some(tls) = &config.tls {
            if !tls.verify_certificate {
                builder = builder.cert_validation(CertificateValidation::None);
            }
        }

        // 配置超时
        if let Some(timeout) = config.request_timeout {
            builder = builder.timeout(std::time::Duration::from_millis(timeout));
        }

        builder.build()
            .map_err(|e| format!("Failed to build transport: {}", e))
    }
}

#[async_trait::async_trait]
impl SearchEngineDriver for ElasticsearchDriver {
    async fn test_connection(&self) -> Result<EsConnectionInfo, String> {
        let response = self.client.info()
            .send()
            .await
            .map_err(|e| format!("Connection failed: {}", e))?;

        let info = response.json::<serde_json::Value>().await
            .map_err(|e| format!("Failed to parse response: {}", e))?;

        Ok(EsConnectionInfo {
            cluster_name: info["cluster_name"].as_str().unwrap_or("").to_string(),
            cluster_uuid: info["cluster_uuid"].as_str().unwrap_or("").to_string(),
            version: info["version"]["number"].as_str().unwrap_or("").to_string(),
        })
    }

    async fn disconnect(&self) -> Result<(), String> {
        // elasticsearch-rs 会在 drop 时自动清理
        Ok(())
    }

    async fn get_cluster_health(&self) -> Result<EsClusterHealth, String> {
        let response = self.client
            .cluster()
            .health(elasticsearch::cluster::ClusterHealthParts::None)
            .send()
            .await
            .map_err(|e| format!("Failed to get cluster health: {}", e))?;

        let json = response.json::<serde_json::Value>().await
            .map_err(|e| format!("Failed to parse response: {}", e))?;

        Ok(EsClusterHealth {
            cluster_name: json["cluster_name"].as_str().unwrap_or("").to_string(),
            status: json["status"].as_str().unwrap_or("unknown").to_string(),
            number_of_nodes: json["number_of_nodes"].as_i64().unwrap_or(0) as i32,
            number_of_data_nodes: json["number_of_data_nodes"].as_i64().unwrap_or(0) as i32,
            active_primary_shards: json["active_primary_shards"].as_i64().unwrap_or(0) as i32,
            active_shards: json["active_shards"].as_i64().unwrap_or(0) as i32,
            relocating_shards: json["relocating_shards"].as_i64().unwrap_or(0) as i32,
            initializing_shards: json["initializing_shards"].as_i64().unwrap_or(0) as i32,
            unassigned_shards: json["unassigned_shards"].as_i64().unwrap_or(0) as i32,
        })
    }

    async fn get_indices(&self, pattern: Option<&str>) -> Result<Vec<EsIndexSummary>, String> {
        let index_pattern = pattern.unwrap_or("*");
        let response = self.client
            .cat()
            .indices(elasticsearch::cat::CatIndicesParts::Index(&[index_pattern]))
            .format("json")
            .send()
            .await
            .map_err(|e| format!("Failed to get indices: {}", e))?;

        let indices: Vec<serde_json::Value> = response.json().await
            .map_err(|e| format!("Failed to parse response: {}", e))?;

        Ok(indices.iter().map(|idx| EsIndexSummary {
            name: idx["index"].as_str().unwrap_or("").to_string(),
            health: idx["health"].as_str().unwrap_or("").to_string(),
            status: idx["status"].as_str().unwrap_or("").to_string(),
            uuid: idx["uuid"].as_str().unwrap_or("").to_string(),
            docs_count: idx["docs.count"].as_str()
                .and_then(|s| s.parse().ok()).unwrap_or(0),
            docs_deleted: idx["docs.deleted"].as_str()
                .and_then(|s| s.parse().ok()).unwrap_or(0),
            store_size: idx["store.size"].as_str().unwrap_or("0b").to_string(),
            primary_shards: idx["pri"].as_str()
                .and_then(|s| s.parse().ok()).unwrap_or(0),
            replica_shards: idx["rep"].as_str()
                .and_then(|s| s.parse().ok()).unwrap_or(0),
        }).collect())
    }

    async fn create_index(&self, request: EsCreateIndexRequest) -> Result<(), String> {
        let mut body = serde_json::json!({});

        if let Some(settings) = request.settings {
            body["settings"] = settings;
        }
        if let Some(mappings) = request.mappings {
            body["mappings"] = mappings;
        }

        self.client
            .indices()
            .create(elasticsearch::indices::IndicesCreateParts::Index(&request.name))
            .body(body)
            .send()
            .await
            .map_err(|e| format!("Failed to create index: {}", e))?;

        Ok(())
    }

    async fn delete_index(&self, index: &str) -> Result<(), String> {
        self.client
            .indices()
            .delete(elasticsearch::indices::IndicesDeleteParts::Index(&[index]))
            .send()
            .await
            .map_err(|e| format!("Failed to delete index: {}", e))?;

        Ok(())
    }

    async fn search(&self, request: EsSearchRequest) -> Result<EsSearchResponse, String> {
        let response = self.client
            .search(elasticsearch::SearchParts::Index(&[&request.index]))
            .body(request.query)
            .from(request.from.unwrap_or(0) as i64)
            .size(request.size.unwrap_or(10) as i64)
            .send()
            .await
            .map_err(|e| format!("Search failed: {}", e))?;

        let json: serde_json::Value = response.json().await
            .map_err(|e| format!("Failed to parse response: {}", e))?;

        let hits = json["hits"]["hits"].as_array()
            .map(|arr| arr.iter().map(|hit| EsSearchHit {
                index: hit["_index"].as_str().unwrap_or("").to_string(),
                id: hit["_id"].as_str().unwrap_or("").to_string(),
                score: hit["_score"].as_f64(),
                source: hit["_source"].clone(),
            }).collect())
            .unwrap_or_default();

        Ok(EsSearchResponse {
            took: json["took"].as_i64().unwrap_or(0),
            timed_out: json["timed_out"].as_bool().unwrap_or(false),
            total: json["hits"]["total"]["value"].as_i64()
                .or_else(|| json["hits"]["total"].as_i64())
                .unwrap_or(0),
            max_score: json["hits"]["max_score"].as_f64(),
            hits,
        })
    }

    // ... 其他方法实现类似
}
```

### 3.3 Tauri 命令

```rust
// backend/src/commands/middleware.rs (扩展)

// ============ Elasticsearch 集群操作 ============

#[tauri::command]
pub async fn es_get_cluster_health(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
) -> Result<EsClusterHealth, String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.get_cluster_health().await
}

#[tauri::command]
pub async fn es_get_nodes(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
) -> Result<Vec<EsNodeInfo>, String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.get_nodes().await
}

#[tauri::command]
pub async fn es_get_shards(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
) -> Result<Vec<EsShardInfo>, String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.get_shards().await
}

// ============ 索引操作 ============

#[tauri::command]
pub async fn es_get_indices(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    pattern: Option<String>,
) -> Result<Vec<EsIndexSummary>, String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.get_indices(pattern.as_deref()).await
}

#[tauri::command]
pub async fn es_get_index_mapping(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    index_name: String,
) -> Result<serde_json::Value, String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.get_index_mapping(&index_name).await
}

#[tauri::command]
pub async fn es_create_index(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    request: EsCreateIndexRequest,
) -> Result<(), String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.create_index(request).await
}

#[tauri::command]
pub async fn es_delete_index(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    index_name: String,
) -> Result<(), String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.delete_index(&index_name).await
}

#[tauri::command]
pub async fn es_open_index(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    index_name: String,
) -> Result<(), String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.open_index(&index_name).await
}

#[tauri::command]
pub async fn es_close_index(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    index_name: String,
) -> Result<(), String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.close_index(&index_name).await
}

// ============ 索引映射和设置修改 ============

#[tauri::command]
pub async fn es_update_index_mapping(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    index_name: String,
    mapping: serde_json::Value,
) -> Result<(), String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.update_index_mapping(&index_name, mapping).await
}

#[tauri::command]
pub async fn es_update_index_settings(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    index_name: String,
    settings: serde_json::Value,
) -> Result<(), String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.update_index_settings(&index_name, settings).await
}

// ============ 文档操作 ============

#[tauri::command]
pub async fn es_get_document(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    index_name: String,
    doc_id: String,
) -> Result<EsDocument, String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.get_document(&index_name, &doc_id).await
}

#[tauri::command]
pub async fn es_create_document(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    request: EsCreateDocRequest,
) -> Result<EsDocResponse, String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.create_document(request).await
}

#[tauri::command]
pub async fn es_delete_document(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    index_name: String,
    doc_id: String,
) -> Result<(), String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.delete_document(&index_name, &doc_id).await
}

// ============ 查询操作 ============

#[tauri::command]
pub async fn es_search(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    request: EsSearchRequest,
) -> Result<EsSearchResponse, String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.search(request).await
}

#[tauri::command]
pub async fn es_sql_query(
    state: State<'_, MiddlewareServiceState>,
    connection_id: String,
    query: String,
) -> Result<EsSqlResponse, String> {
    let session = state.0.get_es_session(&connection_id).await?;
    session.driver.sql_query(&query).await
}
```

---

## 4. 前端实现

### 4.1 类型定义

```typescript
// front/types/middleware.ts (扩展)

// ============ Elasticsearch 类型 ============

export interface EsClusterHealth {
  clusterName: string;
  status: 'green' | 'yellow' | 'red';
  numberOfNodes: number;
  numberOfDataNodes: number;
  activePrimaryShards: number;
  activeShards: number;
  relocatingShards: number;
  initializingShards: number;
  unassignedShards: number;
}

export interface EsNodeInfo {
  id: string;
  name: string;
  ip: string;
  roles: string[];
  version: string;
  heapPercent: number;
  diskPercent: number;
  cpuPercent: number;
}

export interface EsShardInfo {
  index: string;
  shard: number;
  primary: boolean;
  state: 'STARTED' | 'RELOCATING' | 'INITIALIZING' | 'UNASSIGNED';
  docs: number;
  size: string;
  node: string | null;
}

export interface EsIndexSummary {
  name: string;
  health: 'green' | 'yellow' | 'red';
  status: 'open' | 'close';
  uuid: string;
  docsCount: number;
  docsDeleted: number;
  storeSize: string;
  primaryShards: number;
  replicaShards: number;
}

export interface EsDocument {
  index: string;
  id: string;
  version: number;
  source: Record<string, unknown>;
}

export interface EsSearchHit {
  index: string;
  id: string;
  score: number | null;
  source: Record<string, unknown>;
}

export interface EsSearchResponse {
  took: number;
  timedOut: boolean;
  total: number;
  maxScore: number | null;
  hits: EsSearchHit[];
}

export interface EsCreateIndexRequest {
  name: string;
  settings?: Record<string, unknown>;
  mappings?: Record<string, unknown>;
}

export interface EsSearchRequest {
  index: string;
  query: Record<string, unknown>;
  from?: number;
  size?: number;
}
```

### 4.2 服务层 API

```typescript
// front/services/middleware.ts (扩展)

// ============ Elasticsearch Functions ============

// 集群操作
export async function esGetClusterHealth(connectionId: string): Promise<EsClusterHealth> {
  return invoke('es_get_cluster_health', { connectionId });
}

export async function esGetNodes(connectionId: string): Promise<EsNodeInfo[]> {
  return invoke('es_get_nodes', { connectionId });
}

export async function esGetShards(connectionId: string): Promise<EsShardInfo[]> {
  return invoke('es_get_shards', { connectionId });
}

// 索引操作
export async function esGetIndices(connectionId: string, pattern?: string): Promise<EsIndexSummary[]> {
  return invoke('es_get_indices', { connectionId, pattern });
}

export async function esGetIndexMapping(connectionId: string, indexName: string): Promise<Record<string, unknown>> {
  return invoke('es_get_index_mapping', { connectionId, indexName });
}

export async function esGetIndexSettings(connectionId: string, indexName: string): Promise<Record<string, unknown>> {
  return invoke('es_get_index_settings', { connectionId, indexName });
}

export async function esCreateIndex(connectionId: string, request: EsCreateIndexRequest): Promise<void> {
  return invoke('es_create_index', { connectionId, request });
}

export async function esDeleteIndex(connectionId: string, indexName: string): Promise<void> {
  return invoke('es_delete_index', { connectionId, indexName });
}

export async function esOpenIndex(connectionId: string, indexName: string): Promise<void> {
  return invoke('es_open_index', { connectionId, indexName });
}

export async function esCloseIndex(connectionId: string, indexName: string): Promise<void> {
  return invoke('es_close_index', { connectionId, indexName });
}

export async function esRefreshIndex(connectionId: string, indexName: string): Promise<void> {
  return invoke('es_refresh_index', { connectionId, indexName });
}

// 索引映射和设置修改
export async function esUpdateIndexMapping(connectionId: string, indexName: string, mapping: Record<string, unknown>): Promise<void> {
  return invoke('es_update_index_mapping', { connectionId, indexName, mapping });
}

export async function esUpdateIndexSettings(connectionId: string, indexName: string, settings: Record<string, unknown>): Promise<void> {
  return invoke('es_update_index_settings', { connectionId, indexName, settings });
}

// 文档操作
export async function esGetDocument(connectionId: string, indexName: string, docId: string): Promise<EsDocument> {
  return invoke('es_get_document', { connectionId, indexName, docId });
}

export async function esCreateDocument(connectionId: string, request: EsCreateDocRequest): Promise<EsDocResponse> {
  return invoke('es_create_document', { connectionId, request });
}

export async function esUpdateDocument(connectionId: string, request: EsUpdateDocRequest): Promise<EsDocResponse> {
  return invoke('es_update_document', { connectionId, request });
}

export async function esDeleteDocument(connectionId: string, indexName: string, docId: string): Promise<void> {
  return invoke('es_delete_document', { connectionId, indexName, docId });
}

// 查询操作
export async function esSearch(connectionId: string, request: EsSearchRequest): Promise<EsSearchResponse> {
  return invoke('es_search', { connectionId, request });
}

export async function esSqlQuery(connectionId: string, query: string): Promise<EsSqlResponse> {
  return invoke('es_sql_query', { connectionId, query });
}
```

---

## 5. 界面设计

### 5.1 连接配置对话框

```
┌─────────────────────────────────────────────────────────────────┐
│ 新建中间件连接                                            [×]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 连接名称:    [生产环境 Elasticsearch                  ]          │
│                                                                 │
│ 中间件类型:  ○ Redis  ○ Kafka  ● Elasticsearch                 │
│                                                                 │
│ ─────────────────────────────────────────────────────────────── │
│                                                                 │
│ * 集群节点:                                                      │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 请输入集群节点，每行一个，格式：host:port 或 http://host:port │ │
│ │ 例如：                                                       │ │
│ │ 192.168.1.100:9200                                          │ │
│ │ http://192.168.1.101:9200                                   │ │
│ │ https://192.168.1.102:9200                                  │ │
│ └─────────────────────────────────────────────────────────────┘ │
│   格式：每行一个节点，支持 host:port 或 http(s)://host:port       │
│                                                                 │
│ 版本:       [7.x                                      ▼]        │
│             (自动检测 / 9.x / 8.x / 7.x / 6.x / 5.x)            │
│                                                                 │
│ 认证方式:   [基础认证 (username/password)           ▼]           │
│             ○ 无认证  ● 基础认证  ○ API Key  ○ Elastic Cloud    │
│                                                                 │
│ ┌─ 基础认证 ───────────────────────────────────────────────────┐ │
│ │ 用户名:    [elastic                                ]         │ │
│ │ 密码:      [••••••••••                     ] [👁]          │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ ┌─ TLS 设置 ───────────────────────────────────────────────────┐ │
│ │ [✓] 启用 TLS                                                │ │
│ │ [ ] 验证服务器证书                                           │ │
│ │ CA 证书:   [                               ] [选择文件]      │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│         [测试连接]                      [取消]    [保存]        │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 集群概览页面

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 🔌 ZWD-OpsBot                                                           │
├────────────────┬────────────────────────────────────────────────────────┤
│                │                                                        │
│ ▼ 📁 中间件    │  Elasticsearch - 生产环境              [刷新] [断开]   │
│   ▶ 📁 Kafka   │ ┌──────────────────────────────────────────────────┐  │
│   ▼ 📁 ES      │ │  集群概览                                        │  │
│     ● 生产环境  │ │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐│  │
│     ○ 测试环境  │ │  │ 健康状态 │ │ 节点数  │ │ 索引数  │ │ 文档数  ││  │
│   ▶ 📁 Redis   │ │  │ 🟢 green│ │    3    │ │   25    │ │  1.2M   ││  │
│                │ │  └─────────┘ └─────────┘ └─────────┘ └─────────┘│  │
│                │ │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐│  │
│                │ │  │ 活跃分片 │ │ 未分配  │ │ 存储空间 │ │ 版本    ││  │
│                │ │  │   150   │ │    0    │ │  28.5GB │ │  8.11.3 ││  │
│                │ │  └─────────┘ └─────────┘ └─────────┘ └─────────┘│  │
│                │ └──────────────────────────────────────────────────┘  │
│                │                                                        │
│                │ [概览] [索引] [文档] [查询]                             │
│                │ ━━━━━━                                                 │
│                │                                                        │
│                │ ┌─ 节点列表 ───────────────────────────────────────────┐│
│                │ │ 名称          │ IP           │ 角色    │ CPU │ 内存 ││
│                │ │───────────────┼──────────────┼─────────┼─────┼──────││
│                │ │ es-node-1     │ 10.0.0.1     │ master  │ 25% │ 45%  ││
│                │ │ es-node-2     │ 10.0.0.2     │ data    │ 60% │ 72%  ││
│                │ │ es-node-3     │ 10.0.0.3     │ data    │ 55% │ 68%  ││
│                │ └───────────────────────────────────────────────────────┘│
└────────────────┴────────────────────────────────────────────────────────┘
```

### 5.3 索引管理页面

```
┌─────────────────────────────────────────────────────────────────────────┐
│ [概览] [索引] [文档] [查询]                                              │
│        ━━━━━━                                                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ [搜索索引...                    ] [健康: 全部 ▼] [状态: 全部 ▼] [+ 新建] │
│                                                                         │
│ ┌───────────────────────────────────────────────────────────────────────┐│
│ │ [ ] │ 索引名称           │ 健康 │ 状态  │ 文档数  │ 大小    │ 操作   ││
│ │─────┼────────────────────┼──────┼───────┼─────────┼─────────┼────────││
│ │ [ ] │ users              │ 🟢   │ open  │ 125,000 │ 256MB   │ [⋯]   ││
│ │ [ ] │ products           │ 🟢   │ open  │ 50,000  │ 128MB   │ [⋯]   ││
│ │ [ ] │ logs-2024.01       │ 🟡   │ open  │ 1.2M    │ 2.5GB   │ [⋯]   ││
│ │ [ ] │ logs-2024.02       │ 🟢   │ open  │ 800K    │ 1.8GB   │ [⋯]   ││
│ │ [ ] │ .kibana            │ 🟢   │ open  │ 150     │ 2MB     │ [⋯]   ││
│ └───────────────────────────────────────────────────────────────────────┘│
│                                                                         │
│ 已选择 0 项              [刷新] [关闭] [打开] [删除]      共 25 个索引    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

操作菜单 [⋯]:
┌─────────────────┐
│ 📄 查看映射     │
│ ⚙️ 查看设置     │
│ 📊 查看统计     │
│ ───────────────│
│ 🔄 刷新索引     │
│ 🔒 关闭索引     │
│ 🗑️ 删除索引     │
└─────────────────┘
```

### 5.4 查询页面

```
┌─────────────────────────────────────────────────────────────────────────┐
│ [概览] [索引] [文档] [查询]                                              │
│                       ━━━━━━                                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ 查询类型: ● DSL 查询  ○ SQL 查询                                         │
│                                                                         │
│ 目标索引: [users                                               ▼]       │
│                                                                         │
│ ┌─ 查询编辑器 ─────────────────────────────────────────────────────────┐ │
│ │ {                                                                    │ │
│ │   "query": {                                                         │ │
│ │     "bool": {                                                        │ │
│ │       "must": [                                                      │ │
│ │         { "match": { "name": "张三" } }                               │ │
│ │       ],                                                             │ │
│ │       "filter": [                                                    │ │
│ │         { "range": { "age": { "gte": 18 } } }                        │ │
│ │       ]                                                              │ │
│ │     }                                                                │ │
│ │   }                                                                  │ │
│ │ }                                                                    │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│ [格式化] [清空]                                    [执行查询]            │
│                                                                         │
│ ─────────────────────────────────────────────────────────────────────── │
│ 查询结果  (耗时: 15ms, 共 128 条, 显示 1-10)                             │
│                                                                         │
│ ┌───────────────────────────────────────────────────────────────────────┐│
│ │ #1  ID: user_001  Score: 5.234                                       ││
│ │ { "name": "张三", "age": 25, "email": "zhangsan@example.com" }        ││
│ │────────────────────────────────────────────────────────────────────────││
│ │ #2  ID: user_045  Score: 4.891                                       ││
│ │ { "name": "张三丰", "age": 30, "email": "zhangsanfeng@example.com" }  ││
│ └───────────────────────────────────────────────────────────────────────┘│
│                                                                         │
│                              [上一页] 1 / 13 [下一页]                    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 6. 依赖配置

### 6.1 Rust 依赖

```toml
# backend/Cargo.toml

[dependencies]
# Elasticsearch 客户端
elasticsearch = "8.5"

# HTTP 客户端 (elasticsearch 依赖)
reqwest = { version = "0.11", features = ["json", "rustls-tls"] }

# URL 解析
url = "2.5"

# 通用依赖
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
```

### 6.2 版本兼容说明

| Elasticsearch 版本 | 实现方式 | 备注 |
|-------------------|---------|------|
| 9.x | elasticsearch-rs 8.x + REST API | 最新版本，API 向后兼容 |
| 8.x | elasticsearch-rs 8.5+ | 完全支持 |
| 7.x | elasticsearch-rs 7.17+ | 兼容模式 |
| 6.x | REST API 直接调用 | 使用 reqwest 调用 REST API |
| 5.x | REST API 直接调用 | 使用 reqwest 调用 REST API（兼容模式） |

**版本选择说明**：
- 连接时可手动选择 ES 版本，也可选择自动检测
- 自动检测通过调用 ES 根路径 `/` 获取版本信息
- 不同版本的 API 差异主要体现在：
  - ES 5.x: 使用 `_type` 字段
  - ES 6.x: `_type` 字段逐步废弃
  - ES 7.x+: 移除 `_type` 字段
  - ES 8.x+: 默认启用安全认证

**版本选择器选项**：
```
[ 自动检测 ▼ ]
  自动检测
  9.x
  8.x
  7.x
  6.x
  5.x
```

---

## 7. 扩展性设计

### 7.1 添加 OpenSearch 支持

OpenSearch 是 Elasticsearch 的开源分支，API 高度兼容：

```rust
// backend/src/services/middleware/opensearch/driver.rs
pub struct OpenSearchDriver {
    // 可复用大部分 Elasticsearch 代码
    // 主要区别在于认证和某些 API 端点
}

impl SearchEngineDriver for OpenSearchDriver {
    // 实现与 Elasticsearch 相同的 trait
}
```

### 7.2 通用与特有功能

| 通用功能（trait 定义） | Elasticsearch 特有 | OpenSearch 特有 |
|----------------------|-------------------|-----------------|
| 索引 CRUD | SQL 查询 | Alerting |
| 文档 CRUD | Machine Learning | Anomaly Detection |
| 集群健康 | Security (X-Pack) | Security (内置) |
| DSL 查询 | APM | Performance Analyzer |

---

## 8. 验收标准

### 8.1 架构验收

- [x] 后端使用 `SearchEngineDriver` trait 实现策略模式
- [x] 前端复用 `MiddlewareConnection` 类型
- [x] 单个 Rust 文件不超过 800 行（最大文件 295 行）
- [x] 单个函数不超过 80 行
- [x] 连接信息加密存储

### 8.2 功能验收

**连接管理：**
- [x] 支持三种认证方式（无认证、基础认证、API Key）
- [ ] 支持 Elastic Cloud 认证
- [x] 支持 TLS/SSL 连接
- [x] 支持多节点集群连接
- [x] 集群节点使用 textarea 多行输入（每行一个节点）
- [x] 支持 ES 版本选择（5.x-9.x）
- [x] 连接测试功能
- [x] 代理设置选项（可选择直连或使用系统代理）

**集群监控：**
- [x] 集群健康状态显示（green/yellow/red）
- [x] 节点列表和资源监控（CPU/内存/磁盘）
- [x] 分片分配信息

**索引管理：**
- [x] 索引列表（支持搜索、过滤）
- [x] 索引创建（支持设置和映射）
- [x] 索引删除、打开、关闭、刷新
- [x] 查看索引映射、设置、统计
- [x] 修改索引映射（添加新字段）
- [x] 修改索引动态设置（如副本数、刷新间隔）

**文档操作：**
- [x] 文档查看（后端实现）
- [x] 文档创建/更新（后端实现）
- [x] 文档删除（后端实现）
- [x] 批量操作（后端实现）
- [x] 文档管理界面

**查询功能：**
- [x] DSL 查询编辑器
- [x] SQL 查询（ES 6.3+）
- [x] 查询结果分页展示

### 8.3 性能验收

- [x] 连接建立 < 5 秒
- [x] 索引列表加载 < 2 秒（100 个索引）
- [x] 文档查询响应 < 1 秒

### 8.4 i18n 支持

- [x] 中文简体 (zh-CN)
- [x] 中文繁体 (zh-TW)
- [x] 英文 (en-US)
- [x] 日文 (ja-JP)

---

## 9. 文件清单

### 后端文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `backend/src/services/middleware/elasticsearch/mod.rs` | 11 | ES 模块入口 |
| `backend/src/services/middleware/elasticsearch/driver.rs` | 295 | ES 驱动实现（连接、认证、TLS、代理） |
| `backend/src/services/middleware/elasticsearch/cluster.rs` | 271 | 集群操作（健康、节点、分片、统计） |
| `backend/src/services/middleware/elasticsearch/index.rs` | 293 | 索引操作（CRUD、映射、设置） |
| `backend/src/services/middleware/elasticsearch/document.rs` | 269 | 文档操作（CRUD、批量） |
| `backend/src/services/middleware/elasticsearch/query.rs` | 169 | 查询操作（DSL、SQL） |
| `backend/src/services/middleware/traits.rs` | - | SearchEngineDriver trait 定义 |
| `backend/src/commands/middleware.rs` | - | ES Tauri 命令 |
| `backend/src/models/elasticsearch.rs` | - | ES 数据模型 |

### 前端文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `front/components/middleware/elasticsearch/ElasticsearchContainer.tsx` | 262 | ES 主容器（Tab 导航、连接管理） |
| `front/components/middleware/elasticsearch/ElasticsearchOverview.tsx` | 383 | 集群概览（健康、节点、分片） |
| `front/components/middleware/elasticsearch/ElasticsearchIndices.tsx` | 545 | 索引管理（列表、CRUD、映射） |
| `front/components/middleware/elasticsearch/ElasticsearchQuery.tsx` | 377 | 查询分析（DSL、SQL、结果展示） |
| `front/components/middleware/elasticsearch/index.ts` | - | 模块导出 |
| `front/components/middleware/MiddlewareConnectionDialog.tsx` | - | 连接配置对话框（含 ES） |
| `front/services/middleware.ts` | - | ES API 封装 |
| `front/i18n/locales/zh-CN.json` | - | 简体中文翻译 |
| `front/i18n/locales/zh-TW.json` | - | 繁体中文翻译 |
| `front/i18n/locales/en-US.json` | - | 英文翻译 |
| `front/i18n/locales/ja-JP.json` | - | 日文翻译 |

**注：** 所有功能已实现完成，包括文档管理界面 `ElasticsearchDocuments.tsx`。

---

## 10. 参考资料

- [Elasticsearch 官方文档](https://www.elastic.co/guide/en/elasticsearch/reference/current/index.html)
- [elasticsearch-rs 文档](https://docs.rs/elasticsearch/)
- ZWD-OpsBot Kafka 实现：`spec/spec1111.md`
- middleware-manager ES 实现：参考项目
